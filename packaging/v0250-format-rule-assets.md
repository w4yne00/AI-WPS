# v0.25.0 授权格式规则资产包

`build_v0250_format_rule_assets.sh` 生成只包含授权确定性算法适配、编译后的
AI-WPS 权威规则包、`format_semantics.v1` 文本/视觉参考 DSL、来源声明和完整性清单的离线资产包。模板 DOCX 只在构建期
解析；运行时只读加载版本化规则包，不依赖开发目录、在线仓库或第三方默认模板值。

参考 DSL 不包含环境地址或密钥，且仅供人工导入参考；安装器不会自动导入、更新或覆盖已有工作流。

构建前会重新编译模板并与仓库中的规则包逐字节比较，再执行现有 Python 3.8
兼容性扫描和精确白名单审计。资产包状态为 `candidate`，不替代目标机整合版本验收。
