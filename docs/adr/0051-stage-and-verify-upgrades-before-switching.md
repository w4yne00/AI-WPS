# 候选构建验证通过后再切换当前安装

现有安装器在 Adapter 健康检查前已经覆盖正式插件和 Adapter，导致候选构建的 Python 3.8 导入错误同时破坏了旧版可用状态。决定采用事务式升级：候选构建先在隔离目录使用目标运行时完成完整应用导入、启动和健康检查，成功后才切换正式安装；任一步失败都保留或恢复原插件、Adapter 和运行数据，并留下脱敏的阶段诊断。额外安装时间和临时磁盘占用低于现场整体中断和人工恢复的成本。
