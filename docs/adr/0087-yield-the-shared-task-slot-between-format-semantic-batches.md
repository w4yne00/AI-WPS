# 格式语义批次间释放共享任务槽位

决定格式审查每完成一个模型语义批次就保存非敏感进度、释放共享长任务运行槽位并重新参与公平调度；一次初始调用及其重试或纠正构成不可分割批次，批次内部不让位。恢复执行时继续使用任务提交时冻结的执行基线，不重新读取当前配置；必需语义在任务内部优先于可选题注，但格式审查整体仍是普通长任务，不抢占交互任务。十分钟格式语义阶段时限从首次准备调用模型开始按真实墙钟计算并包含批次间排队，排队取消立即生效，时限耗尽后按既定契约降级。该选择会使高负载期间的语义覆盖率下降，但防止一个大型格式审查长期占用两个共享运行槽位之一并将其他宿主任务饿死。
