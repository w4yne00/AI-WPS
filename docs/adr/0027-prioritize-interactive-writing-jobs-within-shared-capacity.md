# 共享模型任务容量内优先调度交互写作任务

智能编写和智能仿写作为交互模型任务进入现有共享协调器。全局同时运行数量仍为 2、排队容量仍为 8；运行中任务不抢占，空闲槽优先分配给排队中的交互任务，同一类别保持 FIFO，并在连续调度 3 个交互任务后安排等待最久的常规任务以防止饥饿。

这一方案避免为 Word 写作另建线程池而突破三宿主总并发边界，同时降低短文本编辑任务长期排在文档审查或 PPT 总结后的概率。代价是队列位置必须按动态调度顺序计算，不能再简单等同于提交顺序。
