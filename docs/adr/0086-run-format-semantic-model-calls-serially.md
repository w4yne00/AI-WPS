# 串行执行格式语义模型调用

决定首版一次格式审查内部的模型调用并发数固定为一：必需格式语义按稳定文档顺序处理，完成后才使用剩余预算生成可选题注；一次调用及其重试或纠正结束前不启动下一批。每次调用边界重新检查用户取消、十分钟阶段时限和十六次调用预算，达到任一终止条件后不再发起请求并按既定降级契约披露未处理候选。该选择可能使慢模型在阶段时限内无法用完调用预算，但避免并发限流、乱序结果、重复纠正和取消后仍有多个在途请求造成的费用与状态不确定性；提高并发必须以后续麒麟目标机及真实工作流、直连服务压测为依据并形成版本化变更。
