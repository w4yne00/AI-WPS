# 正式运行禁止静默返回模型模拟结果

模型配置不完整或模型调用失败时，正式运行必须返回真实错误，不生成看似成功的 mock 结果。mock 只允许在测试或开发环境通过 `AI_WPS_ENABLE_MOCK_PROVIDER=1` 显式启用，并在结果来源中标记“开发模拟”。

格式审查、写作规范、公式检查和 PPT 结构检查中的确定性本地能力仍属于正式降级，不受此限制。这个区分保证模型配置完整度、错误分类和结果来源可信，同时保留自动化与离线开发能力。
