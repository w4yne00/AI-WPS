# Word 审查问题使用可验证原文锚点定位

Word 全篇审查为抽取内容分配由结构单元、段内字符范围和原文校验信息组成的稳定锚点，模型问题必须引用当前分片中的锚点，Adapter 验证锚点范围及原文片段后再生成最终定位。文档在审查快照形成后被编辑时，只有原文片段在预期位置附近唯一匹配才重新定位；无法验证的有价值问题可以保留，但必须标记为定位未验证。

继续使用模型自由生成的“第 N 段”位置无法在分片重叠、跨分片去重和文档后续编辑后可靠指向原文。锚点协议增加了抽取和验证复杂度，并且不能保证对大量重复或已删除文本重新定位，但能够防止错误跳转并使每个已验证问题追溯到固定审查快照。
