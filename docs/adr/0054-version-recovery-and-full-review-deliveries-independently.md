# 恢复构建和全篇审查整合版本使用独立版本号

决定将解除 `v0.23.0-alpha` 现场启动阻断的恢复构建发布为 `v0.23.1-alpha`，其范围包括 Python 3.8 兼容修复、事务式升级、写时复制迁移、恢复模式和升级验收；不得重新打包并继续标记为 `v0.23.0-alpha`。在该修复基线独立验证通过后，再将全篇文档审查及全部修复发布为 `v0.24.0-alpha`。两个版本仍在同一规划和开发主线上推进，但分别保留安装包、日志、验收结果和回退边界。该安排增加一次构建与版本维护成本，却避免故障包与修复包同名，并防止现场恢复被大型新功能的开发和验收周期阻塞。
