# 仅在终态固化后发布格式问题实例

决定格式审查运行期间只公开对象抽取、规则执行、模糊候选、语义批次和剩余时限等非结论性进度，不开放临时问题列表或临时合规结论；问题实例仅在确定性结果与语义补充完成合并、稳定标识固化后发布。用户取消时生成保留二十四小时的取消摘要，记录取消阶段、处理与未处理计数、安全执行基线版本，并固定为执行取消、合规无法判定和覆盖部分，但不发布已经产生的中间问题实例；完整格式快照、候选上下文及未完成内部结果在取消终态立即删除。该选择放弃提前查看部分确定性发现的即时性，以防止问题随后撤销、改类或更换标识，以及用户将取消时的中间数据误认为完整结论。
