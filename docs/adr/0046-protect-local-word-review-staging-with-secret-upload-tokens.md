# Word 全篇审查暂存写操作使用独立秘密上传令牌

Word 全篇审查抽取会话将可公开查询的会话标识与至少 256 位随机强度的秘密上传令牌分离，Adapter 只保存令牌哈希。上传批次、提交快照和删除暂存等写操作必须同时验证令牌、连续序号、内容哈希、锚点范围以及单次和累计请求边界；令牌不进入日志、诊断或错误正文，并在快照提交后失效。

只监听 localhost 不能证明请求来自 WPS，任务号本身也不应兼作写权限凭据；普通网页或同机进程可能尝试向固定本地端口注入或删除内容。秘密令牌不能抵御已经控制当前用户账户的恶意进程，但能在不引入外部认证服务的前提下收紧本地暂存接口的能力边界。
