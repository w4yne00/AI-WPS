# 根据模型调用方式使用固定标准路径

模型配置只让用户填写服务地址，不在普通设置中暴露调用路径。Adapter 为工作流平台固定组合 `/chat-messages`，为平台文档上传组合 `/files/upload`，为模型直连固定组合 `/chat/completions`；最终调用地址在高级诊断中展示。

这一选择牺牲了首版对任意平台自定义路径和 `/workflows/run` 的兼容，以换取三宿主设置一致、降低 `/v1` 与调用路径重复填写风险，并保留现有平台文档上传契约。新增其他平台协议时应增加明确的模型调用方式或协议变体，而不是重新开放 URL 猜测。
