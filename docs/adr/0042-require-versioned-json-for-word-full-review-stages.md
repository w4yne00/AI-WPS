# Word 全篇审查阶段只接受版本化结构化结果

Word 全篇审查的分片和全局汇总阶段只接受通过完整 Schema 校验的版本化 JSON，验证内容包括 Schema 版本、请求与响应分片标识、字段类型与长度、锚点和事实引用以及输出完整性。首次格式错误按固定纠正指令自动重试一次，再次失败则进入可恢复失败；非结构化回复不得作为成功分片、汇总输入或全篇报告展示。

现有限量审查继续保留 JSON 代码块优先、普通 Markdown 明确降级展示的兼容行为。宽松兜底适合单次人工阅读，但全篇审查需要机器合并、稳定定位和饱和检测，接受非结构化中间结果会使覆盖状态和最终报告不可验证。
