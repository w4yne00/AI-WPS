# Word 全篇审查不受任务窗格六十分钟等待上限约束

Word 全篇审查的单次 Provider 请求继续使用 1,800 秒等待上限，但任务整体不再因 WPS 任务窗格等待六十分钟而失败。任务窗格轮询从三秒逐步退避到十秒和三十秒，关闭 WPS 不取消 Adapter 后台任务；用户可通过任务号恢复查看，最终结构化报告的二十四小时保留期从任务完成时开始计算。

多分片任务会在分片之间公平让出运行槽，墙钟时间同时包含模型处理和共享调度等待，不能用固定前端等待时长判断业务失败。单次调用超时、有限自动重试和单任务调用硬上限已经约束异常执行；额外总墙钟超时反而可能把正常排队误判为失败。
