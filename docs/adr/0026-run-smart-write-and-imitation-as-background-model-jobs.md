# 智能编写和智能仿写改为后台模型任务

智能编写和智能仿写迁移到提交、轮询和终态结果协议，不再通过一次同步 HTTP 请求等待模型完成。两项任务获得 `clientJobId` 幂等、配置与输入快照、重开续查和 Adapter 重启中断提示，同时保持智能编写现有对照与写回、智能仿写只预览和复制的行为边界。

只延长同步 Provider 超时会继续依赖浏览器、WPS 与 Adapter 间的长连接，并在断连后留下重复提交风险。旧同步接口保留一个版本作为兼容包装，但正式前端只使用后台任务协议。
