# Word 全篇审查快照通过两遍轻量内容哈希验证

WPS 在第一遍分批抽取并暂存正文与结构化表格时监听编辑事件、记录内容块统计和滚动哈希；提交快照前再按相同顺序执行一遍只读取文本与结构摘要的轻量验证。只有两遍的内容块数量、审查字符数、区域统计和完整哈希一致，且校验结束到提交之间没有编辑事件，才形成审查快照；不一致时删除暂存并要求重新发起。

只依赖 WPS 编辑事件无法覆盖事件漏报和同长度替换，只比较字符数也无法发现段落重排或表格结构变化。第二遍读取增加抽取时间，因此真机六万字符三十秒、十二万字符六十秒目标包含两遍处理，但它避免将抽取期间混合的文档版本提交为一个可追溯快照。
