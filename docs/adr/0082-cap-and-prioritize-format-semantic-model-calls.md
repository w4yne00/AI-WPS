# 限制并分级调度格式语义模型调用

决定一次格式审查最多执行十六次模型调用，候选批次依据实际输入与格式语义输出预算动态缩减，不把固定候选数量视为契约。调用预算先用于会阻断确定性规则的段落角色和对象关系，再用于建议题注；超过预算不得静默丢弃，报告必须披露候选总量、已处理量、跳过量、类别和原因。必需格式语义未处理或未决时，相应规则标记无法判定、格式覆盖状态为部分且合规状态为无法判定；仅建议题注未生成时，确定性覆盖状态不变，语义增强状态为降级。该上限限制了极端文档的语义覆盖率，但为离线目标机上的时延、费用和取消响应提供可预测边界，具体数值应在麒麟目标机压测后作为版本化配置复核。
