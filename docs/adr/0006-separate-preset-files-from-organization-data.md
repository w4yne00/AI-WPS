# 预置规范文件与组织数据分离保存

---
status: accepted
---

版本化预置基线以结构化 JSON 规范包随 adapter 交付，组织覆盖、组织自定义规范和预置停用记录保存在现场 SQLite。Adapter 在运行时合并两层形成生效规范；目标机不联网拉取 `yangqi-tech-writing`，也不依赖 Skill 运行环境。该选择使覆盖安装能够更新预置规范并继续保护现场组织数据，代价是服务层需要实现稳定标识、版本校验、覆盖解析和合并导出。
