# 任务系统提示词作为校验完整性的只读发布资产

八类任务系统提示词以独立 Markdown 和 manifest 随 Adapter 发布。manifest 记录任务映射、版本和 SHA256；模型直连加载前必须通过完整性校验，文件缺失或被修改时相关任务明确失败，不在相同产品版本下静默运行不同核心指令。

目标机组织定制继续通过写作规范库和用户任务要求实现，不直接编辑 System Prompt。覆盖升级可以更新提示词资产，但不得影响模型配置、API Key 或组织规范数据；工作流平台仍使用平台内部已配置的提示词，不因本地文件异常被错误阻断。
