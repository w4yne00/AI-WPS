# 模型调用失败时不跨配置自动降级

模型任务只能使用提交时冻结的模型配置，失败时不得自动切换到另一配置或另一模型调用方式。前台明确说明当前配置和错误类别，由用户主动切换后重新提交；这样模型选择、数据出口、费用和结果来源始终与用户操作一致。

工作流平台为兼容新旧输入节点而执行的 HTTP 400 载荷重试仍被允许，因为请求已被明确拒绝，且重试不改变配置或平台。其他 POST 超时、断连和服务端错误不自动重放，避免服务端已经受理时产生重复任务和费用。
