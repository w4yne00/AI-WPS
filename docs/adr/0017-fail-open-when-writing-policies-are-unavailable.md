# 写作规范不可用时任务降级继续

---
status: accepted
---

预置规范包缺失、版本不兼容、校验失败或组织数据库不可读时，智能编写、智能仿写和文档审查继续使用原有模型链路，只跳过不可用的规范增强并提示人工核对。系统不得自动重建、清空或覆盖组织数据库，也不得改变原有超时、轮询和写回行为；诊断只记录脱敏的规范包、版本、错误代码和阶段。该选择确保辅助规范能力不会拖垮已稳定的核心任务。
