# 每个任务档案独立选择模型调用方式

AI-WPS 不采用影响全部宿主和任务的全局调用方式开关，也不按宿主固定一种调用方式。每个任务档案独立保存平台工作流或模型直连所需的连接参数，同一任务可以保存并切换两类档案；这样可以让现有平台工作流与直接模型长期共存，并保持 Word、Excel、PPT 及其任务之间的配置隔离。

任务提交时必须冻结所选档案的调用方式、接口地址、模型参数和认证快照，后续配置变化不得改变已排队或已运行任务。
