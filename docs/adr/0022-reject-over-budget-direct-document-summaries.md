# 模型直连文档超出输入预算时拒绝处理

模型直连的 PPT 文档总结首版只执行一次模型调用。Adapter 在请求前检查本地抽取文本的模型输入预算；超限时明确拒绝并清理暂存文件，不截断、抽样或自动分块，也不返回可能被误解为全文总结的部分结果。

这一选择牺牲了首版对超长文档的自动处理能力，以保持调用次数、费用、失败状态和结果覆盖范围可预测。多段总结与最终综合如成为刚需，应作为独立长文档能力设计，而不是隐藏在单页或文档总结链路中。
