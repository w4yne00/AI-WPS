# 格式审查自动发送 Word 图片进行视觉语义补充

决定首版格式审查无需用户逐张选择，自动发现 Word 文档中的受支持图片，将其只读导出为绑定格式快照和稳定图对象组的短期图片资产，并通过当前格式审查配置的接入协议发送给后台模型，用于图题存在性核对和建议文案。导出不得转换、移动或修改原 Word 对象，图片资产在任务终态立即删除，不进入报告、日志或诊断；若当前配置不能可靠处理图片，则只允许使用 Alt Text 和邻近文字，证据不足必须返回无法判断，不得声称读取像素或编造图题。该决定把视觉语义补充纳入首版，但不扩展为图片内部文字的完整 OCR、图片内容合规审查或自动写回。
