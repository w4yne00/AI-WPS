# Word 审查问题按原文发生位置建立稳定身份

最终 Word 审查问题使用审查快照、问题类别、排序后的原文锚点和规范化问题语义生成稳定 `issueId`。重叠分片在相同锚点发现的同一问题合并；相同问题出现在不同原文位置时保留为独立问题实例和独立处理状态，并可通过 `duplicateGroupId` 折叠展示。跨章节问题可以作为一个问题实例引用多个证据锚点。

仅按问题文案去重会删除文档中不同位置的真实错误，继续按数组下标标识问题又会在排序、分页、恢复和全局合并后错配处理状态。按原文发生位置建立身份增加了规范化和语义分组逻辑，但能稳定支撑分页、定位、逐处处理和检查点恢复。
