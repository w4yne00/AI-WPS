# 新增模型配置 API 并保留一版工作流档案兼容包装

双模型调用方式使用新的 `/provider/model-configurations` 资源，三宿主新前端只依赖新接口。旧 `/provider/workflow-profiles` 保留一个版本作为兼容包装，只能访问迁移后的工作流平台配置，不能识别或修改模型直连配置；下一主功能版本移除旧接口。

继续复用旧路径会让“工作流档案”长期承担并不属于工作流的直连模型语义，而立即删除又会破坏旧插件与覆盖升级窗口。单版本只读语义边界清晰的包装提供了有限兼容期，同时避免永久双写。
