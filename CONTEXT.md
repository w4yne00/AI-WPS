# AI-WPS 领域词汇

## 当前宿主

用户当前打开并正在使用的 WPS 应用类型，仅指 Word、Excel 或 PPT 中的一种。

## 可用模型配置

已保存当前调用方式所需连接信息和有效认证信息、可以被对应任务选用的模型配置。
_避免使用_：可用工作流、工作流档案

## 模型接口就绪度

当前宿主使用模型能力的准备状态，由接口地址与当前宿主各任务的可用工作流共同决定：

- **已就绪**：当前宿主全部任务均可使用模型能力。
- **部分就绪**：当前宿主只有部分任务可使用模型能力。
- **未就绪**：当前宿主没有任务可以使用模型能力，或尚未提供模型接口地址。

模型接口就绪度只描述当前宿主，不代表其他宿主的配置状态，也不等同于一次真实模型调用的执行结果。
计算模型接口就绪度时，只认定每项任务当前激活的可用模型配置；备用配置和兼容性认证回退不计入用户可见的就绪状态。

## 本地连接状态

WPS 任务窗格对本地 adapter 健康检查的结果。显示“已连接”只表示任务窗格可以访问本地 adapter，不代表模型接口地址、工作流 Key 或模型后台已经可用。本地连接状态与模型接口就绪度必须分别计算和展示。

## 高级诊断

用于故障排查的联调状态与最近任务诊断集合。它不是完成日常配置的必要信息，只有用户主动查看时才展开。

## 任务选项卡

设置界面中用于切换当前宿主不同任务模型配置的导航项。任务选项卡只代表当前版本已经提供的真实功能，不预告或占位展示尚未交付的功能。

## 跨宿主设置界面基线

Word、Excel 和 PPT 设置页共享的布局和交互规范，以现有 Excel/PPT 紧凑设置页为结构参考：顶部状态条、模型接口卡、任务选项卡、配置列表主页、独立配置编辑子页和折叠高级诊断保持相同信息层级；普通控件高度、六像素控件圆角、八像素面板圆角、主次按钮尺寸、选中高亮、焦点环、禁用态和窄窗折行规则保持一致。各宿主只保留自身强调色、任务名称和专属设置内容，不能复制其它宿主颜色或删除 Word 写作规范库。
_避免使用_：完全相同页面、复制 PPT 页面、统一宿主颜色

## 任务模型配置标签

智能编写等任务页模型配置下拉项的唯一显示格式：`配置名称 · 模型调用方式`，其中模型调用方式显示为“工作流平台”或“模型直连”。下拉项不得显示模型标识、API 地址、Key 状态、备注或默认激活符号；当前选中和禁用状态由原生控件状态及辅助反馈表达。设置页配置列表和编辑页仍可显示完成管理所需的模型标识及完整度信息。
_避免使用_：模型名称标签、配置详情、`配置名称 · 模型直连 · 模型标识`

## Ribbon 语义图标

Ribbon 中与一个具体功能一一对应的本地 32×32 PNG 资产。不同功能不得复用同一图标；外部图标必须来自许可明确、可离线再分发的来源，记录原始名称、版本或提交、下载地址、许可证和本地变换，并进入白名单交付、文件哈希及第三方声明。目标机运行时不得联网加载图标。
_避免使用_：在线图标、共享功能图标、来源不明图片

## 模型接入

**模型配置**：
某项任务可选择的一组模型调用设置，包括配置名称、模型调用方式、连接信息、认证信息和该方式所需参数。
_避免使用_：工作流档案、模型档案

**模型调用方式**：
模型请求到达具体模型能力的协议类别，由模型配置明确指定，不根据接口地址自动猜测。
_避免使用_：接入类型、接口模式、URL 类型

**服务地址**：
模型配置中由用户填写的协议服务根地址，不包含由模型调用方式确定的标准调用路径。
_避免使用_：完整接口地址、API URL

**最终调用地址**：
Adapter 将服务地址与模型调用方式对应的标准路径组合后实际请求的地址，只在高级诊断中披露。
_避免使用_：服务地址

**模型配置完整度**：
模型配置是否已经填写当前模型调用方式要求的全部本地字段，不表示模型服务已经连接或调用成功。
_避免使用_：模型连接状态、接口可用状态

**验证调用**：
用户主动触发的一次真实模型请求，用于验证当前任务的模型配置和结果契约，可能产生等待和模型费用。
_避免使用_：刷新配置、连接检测

**默认激活配置**：
某一宿主、某一任务后续新任务默认使用的模型配置。用户在任务页切换选择后立即持久化；已经排队或运行的任务仍使用提交快照。
_避免使用_：全局模型、临时工作流

**配置草稿**：
模型配置编辑页中尚未成功保存的字段变化。配置草稿不参与模型任务或验证调用，也不持久化 API Key。
_避免使用_：临时配置、自动保存配置

**不完整配置**：
已经由 Adapter 保存但尚未具备当前模型调用方式全部必填字段的模型配置。它可以继续维护，但不能激活、验证或提交模型任务。
_避免使用_：无效配置、前端草稿

**迁移模型配置**：
由旧工作流档案在兼容升级中一次性形成的工作流平台模型配置。迁移完成后，它与普通模型配置遵循相同的激活和删除语义，不再由保留的旧字段重新生成。
_避免使用_：旧工作流模式、永久兼容档案

**只读快照**：
任务窗格与 Adapter 暂时断开时保留的最近一次已同步界面数据。只读快照用于维持上下文，不代表实时状态，也不能执行配置变更或模型任务。
_避免使用_：离线配置、可用缓存

**后台模型任务**：
通过提交、状态查询和终态结果获取完成的模型任务，不依赖一次浏览器 HTTP 长连接持续存活。
_避免使用_：异步接口、长连接任务

**交互模型任务**：
用户等待结果以继续当前编辑操作的短文本后台模型任务，当前包括智能编写和智能仿写；它在共享容量内优先于常规长任务获得下一个空闲槽，但不抢占运行中任务。
_避免使用_：短任务、高优先级线程

**任务系统提示词**：
由 AI-WPS 按任务版本化维护的最高层模型指令，定义角色、事实边界和输出契约；模型直连自动使用，工作流平台由运维人员配置同一份内容。
_避免使用_：用户提示词、工作流私有提示词

**工作流平台**：
通过 `/chat-messages` 兼容的模型应用平台接口调用已经编排的应用或工作流，由平台负责模型选择和流程编排。
_避免使用_：Dify、Dify 模式、工作流模式

**模型直连**：
Adapter 使用 OpenAI-compatible Chat Completions 协议直接调用指定模型，不经过工作流平台。
_避免使用_：直接 API、裸模型、直连 Dify

**OpenAI-compatible 接口**：
遵循 Chat Completions 核心请求和响应结构的模型接口。首版兼容不包含厂商原生协议、工具调用或流式输出。
_避免使用_：通用大模型接口、任意模型接口

**本地文档抽取**：
Adapter 在目标机内只读解析 Markdown 或 DOCX，将保留结构的受限文本交给模型直连，不向模型服务上传原始文件。
_避免使用_：本地文件上传、文档转码

**模型输入预算**：
一次模型直连请求可用于任务提示词和用户内容的安全输入容量，必须为模型输出保留空间。超过预算的文档不进入模型请求，也不被静默截断。
_避免使用_：文件大小限制、模型最大字数

## 素材编排

**素材**：
用户从本机选择、用于产出新材料的原始输入文件，当前限于 Markdown 和 DOCX。素材不是当前宿主打开的文档，其内容与当前文档没有必然关系。
_避免使用_：上传文件、附件、原始素材文件

**素材摘要**：
本地文档抽取后得到的受限结构化文本，保留章节层级与表格，不含图片、嵌入对象和版式信息。素材摘要是进入模型请求的唯一素材形式。
_避免使用_：解析结果、文档全文、文档解析器输出

**素材编排**：
以一份素材摘要和一条用户自由指令为输入、由模型一次生成目标材料的 Word 任务，例如方案大纲分工、方案编制、会议纪要、工作总结和运行月报。素材编排每次提交都是一次独立请求，不保留上一次的对话上下文。
_避免使用_：文档对话、多轮问答、智能编写

**素材暂存**：
Adapter 在目标机内对素材文件的受限本地保管，具有大小上限、访问令牌、过期时间和重启清理。素材暂存不向任何模型服务或外部存储传输原始文件。
_避免使用_：文件上传、云端暂存

**提交前预检**：
素材抽取完成后立即执行的本地字数与模型输入预算核对，在发起模型请求之前告知用户是否可以提交。提交前预检不调用模型，也不产生费用。
_避免使用_：预估校验、试算调用

**素材只读、目标文档受控插入**：
素材编排不修改素材文件，也不把当前 Word 文档作为模型上下文；结果可以预览、复制，并仅在用户明确操作时插入当前光标位置，不执行全文替换。这里的“只读”约束素材和默认结果落地方式，不表示目标文档永远不可写。
_避免使用_：只读落地、自动写回、全文替换

**全篇审查任务**：
用户通过一次操作发起、以一份最终审查报告结束的 Word 文档审查任务。系统可以在任务内部按可追溯范围拆分文本、执行多次模型调用并汇总结果；全篇审查任务不等同于单次模型请求。
_避免使用_：单次全文请求、一次模型审查

**全文审查范围**：
首版全篇审查任务承诺覆盖的 Word 可读取区域，包括正文段落、标题、项目符号和编号正文，以及保留行列关系的正文表格。页眉页脚、脚注尾注、批注与修订历史、文本框与形状、图片文字、公式图表、嵌入附件和隐藏文字不属于首版全文审查范围，结果必须披露这些未审查区域。
_避免使用_：DOCX 全部内容、所有 Word 对象

**审查字符数**：
用于判断全篇审查产品容量分级的内容规模，由全文审查范围内去重后的正文和表格可见字符组成。分片重叠、重复表头、Word 控制字符、Adapter 锚点和模型提示词不计入审查字符数；审查字符数不等同于 Word 状态栏字数或模型 Token 估算。
_避免使用_：Word 字数、文件大小、输入 Token

**审查结果保留期**：
全篇审查完成后，用户可以继续分页查看、处理或导出最终结构化报告的本地保留时间。审查结果保留期不延长正文快照或分片中间信息的生命周期；完成后全文和中间事实立即删除，最终结构化报告默认保留二十四小时。
_避免使用_：快照恢复期、永久审查历史

**审查覆盖完整性**：
一次全篇审查任务是否已将目标范围内全部可读取正文纳入某个明确记录的审查分片。它只证明处理范围没有被静默截断，不表示模型能够保证检出全部问题。
_避免使用_：全篇无遗漏、完整检出

**审查分片**：
全篇审查任务中具有明确原文范围和稳定定位信息、可以独立接受模型审查的一段正文。相邻审查分片可以包含少量重叠上下文，但重叠内容不得被重复计入审查覆盖范围或最终问题统计。
_避免使用_：截断文本、抽样片段

**审查快照**：
一次全篇审查任务完成原文抽取和完整性校验后冻结的待审查内容版本。抽取期间检测到正文变化时，本次未完成快照作废；快照完成后，用户继续编辑当前 Word 文档不会改变后台任务的审查范围，结果必须能够说明其对应的快照。
_避免使用_：当前文档、实时正文

**全局一致性汇总**：
全篇审查任务在各审查分片完成后，基于文档结构、分片摘要、受控事实索引和可追溯问题索引执行的跨分片检查与结果合并。全局一致性汇总不重新读取全文，也不能补充未被分片识别和记录的原文事实。
_避免使用_：全文二次审查、全局通读

**原文锚点**：
将审查问题追溯到审查快照具体位置的稳定标识，由内容区域、段落或结构单元、单元内字符范围以及原文校验信息共同确定。当前 Word 文档发生变化后，只有原文能够唯一匹配时才允许重新定位；无法验证的位置必须明确标记，不能伪造段落号。
_避免使用_：模型位置描述、当前光标位置

**审查问题实例**：
最终审查报告中具有稳定问题标识、一个或多个可验证原文锚点以及独立处理状态的问题。同一原文位置被重叠分片重复发现时只形成一个问题实例；相同问题出现在不同原文位置时形成不同问题实例。
_避免使用_：问题数组下标、问题文案

**重复问题组**：
将不同原文位置上语义相同或高度相关的审查问题实例组织到一起的展示与统计分组。重复问题组不取代组内问题实例，也不共享“已处理”或“已忽略”状态。
_避免使用_：合并问题、单一问题

## 格式审查

**格式审查快照**：
一次格式审查从当前 Word 文档只读抽取并冻结的格式事实集合，包括稳定内容块标识、结构关系、可见文本摘要和规则检查所需格式属性。格式审查快照不包含修订历史、批注或其它未声明支持的 Word 对象；快照完成后的文档编辑不改变本次审查依据。
_避免使用_：当前文档格式、实时格式

**格式语义模型**：
将 WPS 原始对象属性规范化后形成的内部文档模型，用统一内容块表示页面、段落、标题、列表、表格、图题、表题、注和附录，并记录原文位置、结构关系、格式事实及数据来源。确定性规则和模型语义补充必须读取同一版本的格式语义模型，不能分别解释原始 WPS 对象。
_避免使用_：模型输入、DOCX 转换模型

**格式事实规范化**：
在 WPS 数据进入格式语义模型的边界，依据事实来源把枚举、point、twip、行距模式、混合值和宿主不支持值转换为带类型、单位和来源的规范事实。规范事实保留原始值供诊断，但规则只能读取规范值；无法确认枚举或单位时标记不可判定，不得用数值大小猜测单位。纸张和页边距仍须正确规范化，但不进入“技术文档模板规则”首版合规判定。
_避免使用_：裸格式值、自动猜单位、显示时临时换算

**格式审查用户显示值**：
格式审查界面、Markdown 报告和复制文本中由规范事实转换形成的中文可读值，例如 `A4`、`25.4 毫米`、`小四（12 磅）`、`两端对齐` 和 `多倍行距`。规则键、枚举码、原始浮点值、英文字段和错误码只进入可展开诊断或机器协议；没有可靠映射时显示无法识别，不得把原始字符直接作为用户结论。
_避免使用_：原始值、接口值、中文协议字段

**格式语义角色**：
独立于具体模板样式名称的受控内容角色，由角色类型和必要属性组成。首版段落角色包括文档标题、标题、正文、列表项、注、题注、目录标题、目录项、附录标题、附录内标题、公式和未知；层级、是否编号及题注类型作为独立属性表达。
_避免使用_：样式名称、`heading1`、`list2_plain`

**格式角色规则映射**：
格式规则包将格式语义角色及属性解析为当前模板规则键的版本化关系，例如标题一级映射为 `heading1`。角色可以被可靠识别但没有映射时，结果为未配置格式规则，不得回退到正文规则。
_避免使用_：模型规则键、角色字符串拼接、正文兜底

**格式角色证据**：
支持判断内容角色且来源可区分的结构、样式、编号、文本模式、文档位置和相邻关系事实。样式名称或格式属性只能构成一种证据，不能单独证明角色；角色确认要求明确结构事实或至少两类相互独立的强证据一致。
_避免使用_：样式即角色、外观即语义、单一规则命中

**格式角色证据冲突**：
当前样式、结构、文本或位置证据指向不同格式语义角色的状态。冲突对象进入模糊格式候选；未达到模型生效门槛时保持需要确认，不允许现有样式自证合规或默认回退正文。
_避免使用_：样式错误、模型兜底、自动选择当前样式

**确定性格式规则**：
根据可验证的格式事实和版本化模板要求执行、相同输入必然得到相同结果的本地规则。格式审查以确定性格式规则为主体；模型不得覆盖、放宽或反转已经能够由确定性规则判定的结论。
_避免使用_：AI 格式判断、提示词规则

**模糊格式候选**：
确定性规则因段落角色、跨块语义关系或事实缺失而不能可靠判定的受限内容块集合。只有模糊格式候选可以进入模型语义补充，且必须携带稳定内容块标识、允许返回的角色或关系类型以及最小必要上下文。
_避免使用_：全文模型审查、疑似问题全文

**模型语义补充**：
模型只对模糊格式候选识别段落角色和语义关系，并可为缺失的图题、表题等内容生成只读补充建议。模型语义补充不直接判断可由确定性规则计算的格式合规性，不修改格式审查快照，也不写回 Word 文档。
_避免使用_：AI 格式审查、自动补全标题

**图像语义补充**：
首版格式审查自动盘点 Word 文档中的全部受支持图片，但只将确定性规则已确认缺少图题、获得可选语义调用预算的图片只读导出，并连同最小必要文字上下文发送给当前格式审查配置以形成图题建议，无需用户逐张选择。已有图题、题注关系未决或未获得预算的图片不得上传像素。该能力不绑定 GLM、Qwen 或其它厂商；只有当前配置的图片输入协议可用时才能声称读取了像素，文本模型不得根据对象存在本身编造图片内容。
_避免使用_：GLM 多模态模式、默认识图、图片 OCR

**图片外发候选**：
确定性规则已确认缺少图题、当前图片输入模式通过视觉能力验证且获得剩余可选格式语义调用预算的可关联图对象。只有图片外发候选才允许创建图片导出槽位；候选按文档稳定顺序选择，未入选对象必须记录已有图题、关联未决、能力不可用或预算不足等原因，不能标记为已经识图。
_避免使用_：全部文档图片、模型选择图片、自动上传图片

**原子图像语义批次**：
以一个可关联图对象组为边界、在一次格式语义调用中完整提交的图片集合，首版最多包含四张图片。组内全部图片必须通过单体门禁且当前配置的视觉能力验证支持该数量；任一图片失败、图片数超限或后台能力不足时不得提交部分像素，整组只能降级为 `text_evidence_only` 或 `not_assessable`。一个原子图像语义批次计为一次格式语义调用。
_避免使用_：逐张生成总图题、部分子图识别、多图共享调用

**按需图像导出**：
必需格式语义完成后，Adapter 依据实际剩余调用预算按文档稳定顺序逐组分配图片导出槽位，WPS 只导出当前原子图像语义批次，Adapter 在该批次调用及重试结束后立即删除 PNG 再处理下一组。图片阶段要求原 WPS 文档和任务窗格可用；关闭文档、退出 WPS、导出响应中断或快照校验失败时，未导出组降级但确定性格式审查继续形成结果。
_避免使用_：预导出全部图片、任务期图片缓存、图片失败即审查失败

**图像导出一致性门禁**：
图片增强阶段开始后，将原文档身份和无编辑状态作为全部剩余图像语义批次的前置条件，并在每组 `SaveAsPicture` 前后验证图对象锚点、类型、数量、尺寸、裁剪、旋转、Alt Text 和邻近文本指纹。检测到任意文档编辑或组指纹变化时，立即停止剩余导出并删除尚未调用的 PNG；不得自动重建快照或混用新图片和原格式快照，确定性审查结果仍可完成。
_避免使用_：图片局部未变、自动刷新快照、继续导出图片

**格式图片资产**：
从 Word 内嵌图片或图片型浮动形状只读导出、绑定稳定图对象组和格式快照指纹的短期视觉输入。导出不得转换、移动或修改原对象；图片资产只用于当前原子图像语义批次，批次调用及其重试结束后立即删除，异常残留最晚在任务终态或会话超时时清理，不进入结构化报告。
_避免使用_：原文附件、永久图片副本、用户选择图片

**WPS 图片导出副本**：
WPS 对受支持格式图片调用 `SaveAsPicture` 生成的临时 PNG 视觉副本。副本不使用原文件名，不等同于原始图片二进制或图片标准化结果；只有目标 WPS 的导出结果通过格式与完整性校验后才能形成格式图片资产。
_避免使用_：OpenXML 原图、Pillow 转码、文档截图

**图片导出槽位**：
Adapter 在格式快照会话下为单个格式图片资产分配的受控临时 PNG 写入位置，由随机图片资产标识和 Adapter 指定路径共同绑定。WPS 只能向该位置调用 `SaveAsPicture`，随后按图片资产标识提交；Adapter 必须拒绝任意调用方路径，并在接收前校验规范路径、普通文件类型、非符号链接、预期属主、PNG 签名、尺寸、大小和 SHA-256。槽位在提交失败、任务终态或会话超时后立即清理，路径和文件名不得进入业务日志或审查报告。
_避免使用_：图片上传路径、任意临时文件、原始图片文件名

**格式图片单体门禁**：
WPS 图片导出副本形成格式图片资产前必须同时满足的资源限制：PNG 文件不超过 `5 MiB`、宽和高任一单边不超过 `8192` 像素、总像素不超过 `2000 万`。任一维度超限时不得上传模型，存在 Alt Text 或邻近文字时仅允许形成 `text_evidence_only`，否则为 `not_assessable`；结构化报告必须披露超限维度，不得静默跳过或在 Adapter 内缩放、压缩。
_避免使用_：模型图片上限、自动压缩图片、忽略超大图片

**受支持格式图片**：
正文故事中由 WPS 明确类型确认的内嵌图片、浮动图片，以及能够安全导出实际像素的链接图片。同一段落的多张图片可以属于一个图对象组，但分别形成格式图片资产；对象所属集合本身不能证明它是图片。
_避免使用_：全部 InlineShape、全部 Shape、页眉水印图片

**不可视觉审查对象**：
首版不导出像素、仅盘点和披露的 Word 视觉对象，包括图表、SmartArt、公式、OLE/ActiveX、文本框、自选图形、组合形状、页眉页脚或水印图片，以及无法安全导出的链接图片。系统不得为取得像素而转换对象类型或修改文档。
_避免使用_：图片、忽略对象、自动转换形状

**图像证据状态**：
说明一条图题建议实际使用了像素、只使用 Alt Text 和邻近文字，或因证据不足无法判断的状态，分别为 `pixel_inspected`、`text_evidence_only` 和 `not_assessable`。状态必须进入结构化报告，模型名称或自报内容不能替代证据状态。
_避免使用_：模型支持图片、已看图、图片已审查

**图片输入模式**：
模型配置明确声明的图片传输协议，首版取值为禁用、OpenAI 兼容 `image_url` 或 Dify 文件上传。图片输入模式不根据厂商或模型名称自动猜测，只有与当前配置修订绑定的视觉能力验证通过后才能用于正式格式审查。
_避免使用_：自动多模态、模型名称推断、通用图片上传

**休眠视觉能力**：
随整合版本交付但在产品和所有模型配置中默认关闭、不会创建图片导出槽位或外发像素的图像语义实现。内网目标机测试条件不足时，休眠视觉能力不构成整合版本发布门禁，也不得宣传为可用；后续完成指定 WPS 环境的图片导出验收、图片外发授权和当前配置视觉能力验证后，管理员可以显式启用，无需变更确定性格式审查流程。
_避免使用_：已支持识图、实验性默认开启、随时直接开启

**图像语义总开关**：
Adapter 本地管理端的 `formatReview.imageSemantics.enabled` 运行时配置，升级和新安装均默认为 `false`。只有管理员确认目标 WPS 图片导出验收完成后才能开启；总开关开启仍需每个模型配置分别完成图片外发授权和视觉能力验证。配置修改只进入后续任务的格式审查执行基线，不改变运行中任务，也不要求重建或重装交付包。
_避免使用_：模型图片模式、任务图片选项、编译开关

**视觉关闭降级**：
图像语义总开关关闭时的非故障运行状态：系统继续盘点图片并执行缺图题、题注位置和格式等确定性规则，但不创建槽位、不调用 `SaveAsPicture`、不上传像素。Alt Text 和邻近文字充分时仍可形成 `text_evidence_only` 建议，否则为 `not_assessable`；报告记录上传数为零和视觉增强未启用，只降低语义增强状态，不降低已完成的确定性格式覆盖状态。
_避免使用_：识图失败、模型不支持、图片未审查

**图片外发授权**：
管理员对某一模型配置修订显式确认、允许将图片外发候选发送至界面所示目标服务的授权状态。授权绑定服务主机、图片输入模式和模型标识，任一项变化即失效；任务发起页必须显示图片外发状态并允许本次关闭。授权只覆盖发送行为，不表示远端不会留存，也不能由接口连通或视觉能力验证代替。
_避免使用_：默认同意图片上传、模型可用即授权、图片已彻底删除

**图片外发记录**：
格式审查结构化报告中关于图片候选数量、实际上传数量、目标服务主机、传输模式和图像证据状态的脱敏事实。记录不得包含图片内容、本地路径、API Key 或请求正文，并必须将本机临时资产清理状态与目标服务控制的远端留存状态分别表述。
_避免使用_：图片上传日志、完整请求记录、远端删除证明

**视觉能力验证**：
使用内置无敏感测试图片和严格响应契约，确认当前模型配置能够通过所声明协议实际识别图片内容的探针。验证结果绑定配置修订、服务地址、接入方式和模型标识，任一项变化即失效；请求成功但未识别校验内容不算通过。
_避免使用_：接口连通测试、模型自报支持、官网能力声明

**格式语义工作流契约**：
Dify 自定义工作流参与模型语义补充时必须实现的版本化输入输出协议，首版为 `format_semantics.v1`。输入固定包含 `contract_version`、`operation` 和 `candidate_json`，视觉操作另接收 `image_files`；输出只从固定变量 `result_json` 读取并按操作对应 JSON Schema 校验。AI-WPS 不修改工作流 System Prompt，接口可达也不表示契约兼容。
_避免使用_：Dify 连接成功、自由文本结果、自动提示词注入

**格式语义契约验证**：
模型配置保存、升级或契约相关字段变化后，使用无敏感合成候选分别验证段落角色分类、题注关联、表题建议和图题建议的过程，图题验证同时承担视觉能力探针。验证调用不计入文档任务预算；旧工作流配置在验证通过前继续保留但只能支持确定性格式审查，失败时提供不兼容字段和改造说明，不自动改写工作流。
_避免使用_：业务文档试运行、自动迁移工作流、模型自报兼容

**格式语义参考工作流**：
随白名单交付包提供、由管理员手动导入 Dify 的 `format_semantics.v1` 示例 DSL，分为文本版和视觉版。文本版支持角色、关联和表题操作，并对图题明确返回不可视觉判断；视觉版支持同组最多四图。参考工作流不包含 URL、密钥或环境数据，不由安装器导入或覆盖，Adapter 的版本化输入输出 Schema 才是运行时权威。
_避免使用_：内置 Dify 工作流、自动部署工作流、运行时工作流依赖

**格式语义输出预算**：
一次格式审查模型语义补充允许生成的有效 Token 上限。工作流平台采用该工作流的明确输出限制；模型直连优先采用当前配置，未填写时按精确模型标识匹配随版本发布的官方能力上限，但最终仍受格式审查任务上限约束。
_避免使用_：模型上下文窗口、官网默认输出、模型最大输出

**格式模型能力表**：
随 AI-WPS 版本离线发布、按精确模型标识记录官方最大输出能力及来源日期的受控映射。无法唯一匹配模型时不得根据厂商名称猜测能力，确定性格式审查仍可执行，模型语义补充保持未就绪。
_避免使用_：在线模型探测、GLM 通用上限、服务端默认值

**格式语义调用预算**：
一次格式审查允许执行的模型语义补充调用总量，首版上限为十六次。预算优先用于会影响规则执行的角色与关系判断，剩余预算才用于建议题注；任何超额候选都必须记录数量、类别和未处理原因。
_避免使用_：无限模型调用、静默跳过、固定候选批次

**格式语义调用序列**：
一次格式审查内部按稳定候选顺序串行执行的模型请求序列，首版并发数固定为一。每次调用及其重试或纠正结束后必须重新检查取消、阶段时限和剩余预算，必需格式语义完成后才进入可选格式语义。
_避免使用_：并行模型批次、自适应并发、乱序候选

**格式语义协作让位**：
一个完整语义批次结束后保存进度、释放共享长任务运行槽位并重新参与公平调度的状态转换。调用及其重试或纠正构成不可分割批次；让位后继续使用原格式审查执行基线，不重新读取当前配置。
_避免使用_：持续占用任务槽、重新提交任务、切换模型配置

**必需格式语义**：
确定性规则继续判定所必需的段落角色或对象关系。必需格式语义因调用预算、模型故障或低置信度而未决时，相关规则无法判定并使格式覆盖状态变为部分。
_避免使用_：题注文案建议、可选增强

**可选格式语义**：
不影响确定性合规判定的模型补充内容，首版主要是对已确认缺少题注的图表生成建议题注。未执行可选格式语义只使语义增强状态降级，不改变确定性覆盖状态。
_避免使用_：必需角色判断、合规依据

**格式语义阶段时限**：
一次格式审查中全部模型语义调用、重试、纠正及批次间重新排队共享的墙钟等待上限，首版为十分钟；单次调用最长六十秒。阶段时限耗尽后不再发起请求，尚未解决的候选按必需格式语义或可选格式语义分别降级。
_避免使用_：单次模型超时、格式审查总时限

**格式语义纠正调用**：
模型响应无法通过完整 JSON Schema 时，携带受控校验错误发起的一次结构纠正请求。纠正调用和瞬时故障重试均计入格式语义调用预算；纠正后仍不合格时整批响应拒绝，不从无效响应中选择部分操作。
_避免使用_：本地猜测修复、部分 JSON 接受、无限重试

**格式审查执行基线**：
格式审查任务提交时冻结的模型配置修订、非持久化认证快照、System Prompt、格式规则包和格式模型能力表版本集合。同一任务的全部确定性规则、模型调用和报告必须引用同一执行基线；运行期间的配置切换或删除只影响后续任务。
_避免使用_：实时模型配置、当前激活配置、混合规则版本

**格式语义输入预算**：
一次模型语义调用允许携带的估算输入 Token 上限，首版任务上限为 `8192`。工作流平台为不可见的内部提示词与节点处理预留容量；模型直连还必须扣除 System Prompt、有效输出预算和百分之十安全余量，并采用两种上限的较小值。
_避免使用_：模型完整上下文、整篇文档输入、固定候选数量

**语义输入超限候选**：
携带最小必要文本、结构关系和格式事实后仍无法放入格式语义输入预算的完整候选。候选不得静默截断或拆成失去共同上下文的独立判断；必需候选超限使相关规则无法判定，可选候选超限只使语义增强降级。
_避免使用_：截断候选、分段猜测、跳过候选

**格式审查覆盖完整性**：
一次格式审查是否已将本次声明支持的全部 Word 内容块纳入格式审查快照，并对每个内容块记录已检查、无需检查、数据不足或不支持的明确状态。它不表示规则覆盖所有格式标准，也不表示模型能够检出全部语义问题。
_避免使用_：格式全部正确、完整检出

**授权规则内置分支**：
将已经取得作者授权的第三方确定性规则按固定来源版本纳入 AI-WPS 源码和白名单交付包，经适配后由 Adapter 内部调用的受控代码副本。内置分支必须记录来源版本、文件哈希、授权说明和本地改造，满足 Python 3.8、离线运行及发布门禁；目标机不得依赖开发目录、在线仓库或未经验证的上游更新。
_避免使用_：外部规则目录、在线规则依赖、原仓库直连

**格式审查承诺范围**：
首版格式审查必须全量处理的 Word 可读取对象，包括页面与节属性、正文段落及字符格式、标题、列表、正文表格、图与表题、目录、注和附录；页眉页脚正文及页码采用尽力读取，读取失败时标记数据不足。文本框、形状、SmartArt、公式内部、脚注尾注、批注、修订历史、隐藏文字和图片文字首版只统计并披露，不承诺内部格式审查。
_避免使用_：全部 Word 对象、完整 DOCX 格式

**不支持对象清单**：
格式审查快照中对承诺范围之外对象记录的类型、数量和读取状态，不保存不必要的内部内容。结果必须展示不支持对象清单，避免把未检查区域误报为合规。
_避免使用_：忽略对象、无格式问题

**格式快照会话**：
WPS 为一次格式审查创建、分批上传格式语义块并在完整性校验后提交的短期本地会话。会话沿用秘密上传令牌、连续批次、请求体上限、累计对象上限、哈希校验和过期清理等安全约束，但使用独立于内容审查的格式快照数据契约。
_避免使用_：单次格式请求、全文 JSON

**增量格式抽取**：
WPS 按稳定文档顺序读取有限数量对象，生成并上传一个格式快照批次，然后主动让出任务窗格事件循环再继续下一批的过程。增量格式抽取不得为了最终上传而在前端保留另一份完整快照，也不得以批次边界截断表格、题注关联或其它结构关系。
_避免使用_：同步全文扫描、抽样检查

**格式指纹**：
按稳定对象顺序对文本摘要、内容块类型、结构关系、参与审查的规范化格式属性及覆盖统计计算的版本化哈希。格式指纹不包含运行时地址、模型配置或其它与文档格式无关的数据。
_避免使用_：文件哈希、文档名称哈希

**两遍格式指纹校验**：
第一遍分批上传完整格式事实后，第二遍重新读取相同范围并只计算结构与格式指纹、对象数量和覆盖统计的快照一致性门禁。两遍任一关键值不一致时必须清理未提交快照并要求停止编辑后重试；编辑信号只能用于提前停止，不能替代第二遍校验。
_避免使用_：单次编辑状态检查、抽取成功

**模型语义操作**：
模型针对模糊格式候选可以返回的版本化白名单操作，首版仅包括段落角色分类、题注关联、图题建议和表题建议。操作必须绑定格式审查快照指纹及稳定块标识，并通过结构、候选范围、允许目标和置信度校验。
_避免使用_：模型修改、AI 规则

**需要确认的格式角色**：
确定性证据不足且模型结果未达到生效门槛的内容块状态。该内容块不得默认按正文或其它角色执行角色专属合规判断；结果应展示候选角色和核对提示。
_避免使用_：正文兜底、低置信度角色

**建议题注**：
模型为已由确定性规则确认缺少题注的图或表生成的候选正文。建议题注不包含“图/表”前缀、章节号、顺序号或分隔符；这些展示元素由确定性规则根据格式快照计算。建议题注始终属于只读建议，不构成原文事实、合规修复或自动写回内容。
_避免使用_：生成题注、已补全标题

**题注建议契约**：
`suggest_figure_caption` 和 `suggest_table_caption` 的结构化输出约束：题注正文最长八十个 Unicode 字符，不含 Markdown、换行、解释性前后缀、题注类型前缀和编号，并携带图像或表格证据状态、简短证据说明、置信度及无法确认信息。出现不受证据支持的具体事实、编号或格式字符时拒绝整条建议，不进行猜测性字符串修复。
_避免使用_：完整图题、模型编号、自动清洗题注

**表题证据视图**：
为已确认缺少表题的数据表构造的受控模型输入，包含完整多级表头与合并关系、行列数、单位、来源、表下注、所在标题和紧邻上下文；完整单元格在输入预算内时全部提供，超限时只提供明确标记的首三行和末两行样例。证据视图必须声明完整或受限状态，受限状态不等同于语义输入超限候选的静默截断。
_避免使用_：表格全文、未标记抽样、表格截图

**受约束表题建议**：
模型依据表题证据视图形成的只读候选文案，必须携带证据范围，且不得引入证据中没有出现的机构、时间、地域、数值或统计口径。结构或内容校验失败时整条建议拒绝；证据不足时返回无法可靠建议，不得输出泛化标题。
_避免使用_：自动表题、模型概括表格、默认情况表

**可关联图表对象**：
题注关联的稳定目标；每张表形成一个表对象，同一段落内的一个或多个内嵌图片形成一个图对象组。首版每个对象只允许一个主题注，复杂子图题和共享总题只盘点并提示人工核对。
_避免使用_：图片文件、单个绘图节点、任意形状

**题注关联**：
在同一节和正文故事范围内，将现有题注绑定到唯一兼容可关联图表对象的语义关系。关联是否成立与题注位置是否合规分别判断；表题在表下或图题在图上仍可确认关联，并单独形成位置违规。
_避免使用_：题注相邻、题注位置、自动移动

**题注关联歧义**：
现有题注对应多个兼容对象、存在重复题注或缺少唯一可信候选边的状态。歧义未解决时题注标记孤立或待关联，对象的缺题注状态为无法判定，不能同时确定为缺失。
_避免使用_：最近对象兜底、重复缺题注、模型自由关联

**格式标准权威源**：
一次格式审查用于判定期望值的唯一活动规则包“技术文档模板规则”。其格式值来自固定 `wx-doc-format 0.12.15` 快照的模板资产，结构约束来自经过人工准入的规则白名单；模板资产中的明确格式值优先，未定义属性保持未配置。转换器成品合同、写回逻辑和代码 fallback 不得成为审查标准，模型不得生成、推断或修改格式标准。
_避免使用_：旧技术文件模板、全部转换器规则、模型格式标准

**通用结构规则**：
不依赖某一模板具体样式值、经过 AI-WPS 人工登记确认后启用的结构约束，例如标题层级不得无依据跳跃或题注与对象的相对位置。第三方代码中看似通用的行为在登记前仍不能作为合规依据。
_避免使用_：默认规则、行业惯例自动生效

**未配置格式规则**：
格式审查已经读取到对象属性，但当前格式标准权威源没有提供该属性的期望值或结构约束时的状态。未配置格式规则不得使用第三方默认值补齐，也不得将对象判为合规或违规。
_避免使用_：沿用默认值、无需检查

**表格语义类型**：
决定表格适用哪些专属格式规则的受控角色，首版包括数据表、代码示例、说明框、排版表和未知。多单元格本身不是数据表的充分证据；未知表格只执行不依赖语义类型的通用规则，并披露数据不足。
_避免使用_：多单元格数据表、表格样式名称、模型表格分类

**数据表证据**：
支持将表格确定为数据表的可验证结构事实，例如明确表头、重复行结构、稳定列语义或多行记录。缺少数据表证据时不得以默认置信度、外观相似或第三方转换习惯触发数据表专属格式和缺表题规则。
_避免使用_：表格有多个单元格、看起来像数据、默认数据表

**格式规则包**：
由权威模板 DOCX 中可验证提取的格式值与人工审定的结构规则清单在构建期编译生成、供 Adapter 运行时只读加载的版本化规则集合。规则包记录源模板哈希、规则来源、适用对象、单位、容差、严重级别、启用状态、授权算法版本及本地适配版本。
_避免使用_：模板 JSON、运行时模板解析结果

**技术文档模板规则**：
格式审查唯一活动主规则包的中文显示名称；稳定机器标识为 `technical-document-template-rules`，首个规则版本为 `1.0.0`，上游来源版本单独记录为 `wx-doc-format 0.12.15`。来源规则逐条分类为格式规范、结构规范或转换器专用，只有前两类可以进入编译白名单；纸张类型和四边页边距不属于首版合规规则。旧规则资产只作历史审计并标记为不活动，不得出现在界面或被运行时回退加载。
_避免使用_：技术文档模板规则 1.0.0、wx 规则包、默认模板

**结构规则清单**：
无法仅从 DOCX 样式定义可靠推导、由人工明确审定的标题层级、列表连续性、目录、题注位置、表格语义、注和附录等约束。结构规则清单必须经过构建期校验后才能进入格式规则包。
_避免使用_：代码硬编码、第三方默认行为

**规则包差异报告**：
模板 DOCX、结构规则清单或授权算法版本发生变化时，由构建生成的规则增删改和来源变化清单。存在未确认差异、冲突、缺失来源或不可解析值时，构建不得发布新规则包。
_避免使用_：模板更新日志、代码差异

**格式审查语义范围**：
用户选区按完整格式语义单元扩展后形成的实际问题报告范围。部分段落扩展为完整段落，表格单元格扩展为完整表格，图表与题注、列表项和标题读取最小必要相邻上下文；上下文只参与判定，不得产生范围外问题。
_避免使用_：原始字符选区、上下文审查范围

**选区格式上下文**：
为判断格式审查语义范围内对象而额外读取、但不属于问题报告范围的相邻结构块和所在节属性。选区格式上下文必须标记为仅供判定，不计入已审查对象数量，也不能产生独立问题。
_避免使用_：扩展审查范围、全文背景

**格式问题位置描述**：
将格式问题的已验证原文锚点转换为用户可核对的位置表达。段落级问题优先显示章节路径、段落序号、原文摘要，并在 WPS 能验证布局时附加审查快照页码；节级问题显示节或章节及可用页码范围，文档级问题显示全文。快照页码只是布局提示，不能替代稳定锚点；无法取得页码时不得把已有章节或段落位置降级为位置未确认。
_避免使用_：固定页码、伪造段落号、统一位置待确认

**未执行全局格式规则**：
因选区模式缺少完整文档范围而不能可靠执行的目录完整性、全文标题连续性、全文编号连续性等规则集合。结果必须列出未执行全局格式规则，不得将其视为通过。
_避免使用_：选区无需检查、全局规则通过

**格式合规状态**：
格式审查对已执行规则的结论，取值为通过、发现违规或无法判定。格式合规状态必须与执行状态、覆盖状态和语义增强状态分别报告；问题数量为零不能单独形成“通过”。
_避免使用_：任务成功、零问题

**格式覆盖状态**：
格式审查承诺范围内对象和必需规则是否均获得明确处理状态，取值为完整或部分。首版不支持对象已经完成盘点和披露时不使覆盖状态变为部分；承诺范围内对象读取失败、规则未执行或必需语义未决时必须披露。
_避免使用_：扫描完成、对象数量正常

**格式审查通过**：
执行已经完成、格式覆盖状态完整、全部必需规则已执行、没有未解决模糊角色且没有格式违规时才能显示的严格结论。模型语义补充仅在存在必需模糊候选时影响通过条件。
_避免使用_：未发现明显问题、零条问题

**格式问题实例**：
格式审查报告中由规则标识、稳定格式锚点和属性路径唯一标识的一处具体违规或待核对项。格式问题实例记录结构化当前值与期望值、单位、容差、证据、数据状态、建议、规则与模板版本，以及模型语义来源和置信度；展示文案不能作为问题身份。
_避免使用_：问题数组下标、问题文案

**稳定格式锚点**：
将格式问题追溯到格式审查快照具体节、内容块、字符区段、表格行列单元格或图表对象的位置描述，并附带文本与结构校验信息。稳定格式锚点不能只使用易随编辑变化的段落序号。
_避免使用_：段落号、模型位置描述

**格式重复问题组**：
按规则、期望值和相同问题特征组织多个格式问题实例的统计与展示分组。组不会替代实例，也不能让不同位置共享定位或处理状态。
_避免使用_：合并问题、批量单一问题

**格式审查结构化报告**：
包含多维结果状态、覆盖清单、分页格式问题实例、重复问题组、未执行规则、数据不足、不支持对象和语义增强诊断的版本化结果。Markdown 只由结构化报告生成，用于复制或导出，不是运行时唯一数据源。
_避免使用_：Markdown 结果、问题列表文本

**格式审查运行进度**：
终态前可以公开的非结论性状态，只包含抽取对象、规则、模糊候选、语义批次和剩余时限等计数及阶段。运行进度不得返回临时问题实例、临时合规结论或尚未固化的问题标识。
_避免使用_：实时审查结果、临时问题列表、流式报告

**格式审查取消摘要**：
用户取消后保留的终态非结论性记录，包含取消阶段、已处理和未处理计数、安全执行基线版本，以及执行取消、合规无法判定和覆盖部分状态。取消摘要不包含问题实例，并按格式报告保留期删除。
_避免使用_：部分审查报告、取消结果、已发现问题

**格式问题定位**：
用户从格式问题实例发起、在当前 Word 文档中只读选中对应对象的操作。定位前必须验证文档身份、原始范围、文本指纹和相邻结构；原位置偏移时只允许在有限邻域唯一匹配后重定位，无法唯一验证时拒绝跳转。
_避免使用_：按段落号跳转、强制定位

**失效格式锚点**：
当前文档变化后无法与格式审查快照中的稳定格式锚点形成唯一可信匹配的状态。失效格式锚点仍保留报告证据，但不能用于自动选择文档位置。
_避免使用_：位置不存在、自动猜测位置

**格式快照保留期**：
格式快照会话及完整格式事实在本地暂存的最长生命周期。未提交会话默认十分钟过期；已提交快照仅保留到任务完成、失败或取消，终态后立即删除完整文本、格式块和模型候选上下文。
_避免使用_：报告保留期、永久快照

**格式报告保留期**：
格式审查终态后分页结构化报告在本地可继续查看、定位或导出的时间，默认二十四小时。报告只保留问题实例、必要证据短片段、锚点校验信息、统计和脱敏诊断，不延长完整格式快照的生命周期。
_避免使用_：格式快照保留期、审查历史

**格式区段**：
段落或表格单元格内部具有一致字符格式属性的一段连续 Range，以内容块内相对字符范围和规范化格式事实表示。相邻且属性相同的格式区段必须合并，不重复保存全文文本。
_避免使用_：字符对象、文本分片

**自适应格式区段抽取**：
先读取完整 Range 的字符格式，遇到 WPS 混合或未定义值时递归二分，直到得到同质格式区段，再合并相邻相同区段的抽取算法。极端碎片化超过安全上限或目标 WPS 无法可靠分割时，必须标记字符格式数据不足，不能以段落平均值或静默截断替代。
_避免使用_：逐字符扫描、段落字体

**格式审查容量级别**：
依据审查字符数形成的产品容量分级，六万及以下为标准文档，六万以上至十二万为大型文档，超过十二万首版明确拒绝。容量级别必须与内容块、表格单元格、格式区段和累计快照字节等对象复杂度门禁联合使用。
_避免使用_：文件大小、仅按 Word 字数

**格式对象复杂度门禁**：
针对内容块、表格单元格、格式区段、不支持对象和累计快照字节数设置、通过麒麟真机基准确定的安全上限。任一维度超限时停止并清理快照，返回实际值和上限；不得截取部分对象后继续判定。
_避免使用_：隐藏截断、固定段落上限

## 升级与发布

**产品成熟度通道**：
描述一个产品版本面向用户的稳定程度，当前使用 `alpha`、`preview`，未来达到正式发布门禁后再定义稳定通道。产品成熟度通道不证明某个具体构建已经通过自动化或目标机验收。
_避免使用_：发布状态、验收状态、候选状态

**交付验证状态**：
描述某个具体构建的验证结果，使用 `candidate`、`target-accepted` 或 `rejected`。交付验证状态与产品成熟度通道正交，例如 `preview` 版本仍可处于 `candidate` 状态。
_避免使用_：Preview 即已验收、Alpha 即不可安装

**v0.25.1-alpha 格式审查收敛线**：
沿用现有 Phase1 安装体系、只修复格式审查正确性、模型调用诊断和目标机验收阻断项的最后一个 Alpha 候选。该版本不引入素材编排，也不建立新的安装基线。
_避免使用_：Preview 首版、素材编排版本、Phase1 新基线

**v0.26.0-preview.1 预览基线**：
在 `v0.25.1-alpha` 目标机验收完成后建立的首个 Preview 产品版本，使用中性包名、安装根、安装脚本和发布标识，并成为此后兼容升级和增量升级包的共同基线。
_避免使用_：v0.25 Preview、Phase1 Preview、已验收预览版

**一次性安装基线断代**：
从 Phase1 安装体系切换到 `v0.26.0-preview.1` 时执行的一次人工重新安装和配置。系统不自动迁移旧安装根、参数、API Key 或运行数据，不自动删除旧目录，也不承诺旧入口兼容；此边界只适用于进入 Preview 的这一次转换。
_避免使用_：自动升级、兼容升级、目录改名

**兼容升级**：
以 `v0.26.0-preview.1` 或其后明确基线为起点，在保留配置和运行数据的前提下完成程序、协议和数据迁移的后续版本升级。兼容升级失败时必须保留可恢复的原基线，不得要求用户通过全量重装作为常规恢复路径。
_避免使用_：覆盖安装、重新配置、全量重装

**增量升级包**：
声明起始基线、目标版本、变更文件、迁移步骤、完整性校验和回退条件的受控差量交付物。增量升级包只适用于明确兼容的起始基线，基线不匹配时必须拒绝，不得猜测或跳过迁移。
_避免使用_：补几个文件、通用补丁包、任意版本升级包

**恢复构建**：
用于解除已部署版本启动阻断、恢复既有能力和运行数据可访问性的独立可安装构建。恢复构建不等待同一开发主线中的新功能完成。
_避免使用_：临时包、随手修正版

**整合版本**：
在恢复构建验证通过的基线上合入新功能并完成完整验收的后续版本。整合版本可以统一交付修复和功能，但不能覆盖恢复构建的独立身份与验证记录。

**0.24.x 文档审查维护线**：
以 `v0.24.0` Word 全篇文档审查为功能基线、只通过 `0.24.1`、`0.24.2` 等补丁位修复文档审查及其直接依赖缺陷的版本线。该维护线不承载计划中的格式审查、图片语义或三宿主体验新功能；补丁修复必须前向合并到后续功能版本。
_避免使用_：v0.24 格式审查版本、0.24 新功能线、格式审查整合版本

**v0.25.0 格式审查功能线**：
在已完成目标机验收的最新适用 `0.24.x` 基线上，交付确定性优先格式审查、受限模型语义、默认关闭的图片语义能力及相关三宿主体验改造的后续功能版本。该版本不得作为 `0.24.x` 文档审查缺陷修复的等待条件。
_避免使用_：v0.24 格式审查、文档审查补丁版、无门禁格式审查开发

**前代验收开发门禁**：
以后续功能依赖的前一功能版本目标机验收 Issue 状态作为代码开发前置条件的状态型门禁。`v0.25.0` 的门禁为 GitHub Issue #42 处于 `CLOSED`；门禁覆盖生产代码、测试代码、协议、资源、清单和打包修改，但不阻止规格、ADR、tickets 和只读调研。Issue 重新打开时暂停新的实现与集成交付。
_避免使用_：口头验收、计划关闭、开发中再验收、日期门禁
_避免使用_：覆盖版、同一个修复包

**事务式升级**：
在切换当前可用安装前，先隔离验证候选构建；只有目标运行时、启动和健康检查全部通过才完成切换，任一步失败都保留或恢复原安装和运行数据。
_避免使用_：直接覆盖、先装再测

**恢复模式**：
Adapter 进程和诊断接口保持可达，但一个或多个持久化子系统因数据读取或迁移失败被隔离为只读的受限运行状态。恢复模式允许执行健康检查、脱敏诊断、备份和显式恢复；不得重置、覆盖或删除原数据，也不得执行依赖故障子系统的模型任务。
_避免使用_：正常启动、空配置模式、自动修复

**写时复制迁移**：
升级时先对原运行数据生成脱敏清单和校验信息，再复制到隔离位置并只在副本上执行迁移；只有副本通过结构、数量、引用关系和完整性验证后才原子切换。迁移失败时原数据保持不变，API Key 仅校验存在性和不可逆指纹，不记录或输出明文。
_避免使用_：原地升级、自动修复数据

**目标运行时门禁**：
使用正式支持的最低目标运行时，对最终交付包执行完整应用导入、真实进程启动、关键接口、升级数据和失败保护验证的阻断式发布检查。源码编译、静态扫描或更高版本运行时测试不能替代目标运行时门禁。
_避免使用_：语法检查、开发机冒烟测试

**候选构建**：
已经通过自动化目标运行时门禁、允许安装到测试目标机，但尚未完成正式目标操作系统和 WPS 现场验收的构建。候选构建不得被宣称为已解除现场故障。
_避免使用_：正式修复版、已交付版本

**已验收恢复构建**：
候选恢复构建在正式麒麟、WPS 和目标登录用户环境中完成真实升级、运行数据保持、功能恢复及重启保持性验收后的状态。
_避免使用_：本地测试通过、候选包

**安装主体一致性**：
安装器在写入和切换前确认目标 WPS 用户、插件目录、Adapter 目录和运行数据目录属于同一明确 UID，目标主目录真实存在且相关目录可写。校验失败时安装必须保持原状态并停止，不得通过静默递归修改属主绕过问题。
_避免使用_：当前 shell 用户、自动修复权限

**存活状态**：
Adapter 进程和基础 Web 应用是否能够响应请求。存活不表示模型配置、写作规范或模型服务已经就绪。
_避免使用_：服务正常、功能可用

**业务就绪状态**：
Adapter 的必需持久化子系统是否均可安全读取并提供业务操作。恢复模式下进程可以存活，但业务就绪状态必须为未就绪。
_避免使用_：端口可达、健康状态

**降级状态**：
Adapter 的核心任务能力仍然就绪，但写作规范等非核心增强子系统不可用的运行状态。降级状态下相关任务按既有降级契约继续执行并明确提示能力缺失，故障数据保持只读，管理操作禁用。
_避免使用_：恢复模式、服务正常、静默忽略

**子系统状态**：
模型配置、写作规范等 Adapter 子系统分别报告的 `ready`、`degraded` 或 `recovery` 状态，附带稳定错误码、发生阶段和允许的恢复动作，不包含密钥、配置正文或敏感绝对路径。
_避免使用_：异常文本、原始堆栈

**恢复模式激活**：
当前安装已经不就绪且候选构建只能以恢复模式存活时，在原数据备份校验通过后由用户显式执行的受限切换。恢复模式激活只开放诊断、备份和恢复能力，不代表升级成功或恢复构建已经验收。
_避免使用_：强制升级、降级安装、升级成功

**脱敏诊断包**：
用于现场排障的可导出材料，仅包含版本、运行时、阶段、稳定错误码、子系统状态、数据数量、不可逆指纹和经过清洗的日志；不得包含 API Key、配置正文、文档内容或敏感绝对路径。
_避免使用_：完整日志、配置备份

**显式恢复命令**：
以已校验备份 ID 为输入、要求二次确认并生成审计记录的数据恢复操作。显式恢复命令不得隐式选择备份，也不能以空数据重置替代恢复。
_避免使用_：一键修复、恢复默认值

**一致性运行数据快照**：
在同一时点对模型配置、八类任务映射、激活关系、API Key 文件和写作规范数据库形成的整体备份，附带版本、数量和校验清单。快照只能在副本验证通过后整体原子恢复，默认不允许拆分恢复单个文件。
_避免使用_：配置备份、数据库副本、局部恢复包

**最后有效快照**：
已经通过结构、引用关系和完整性校验，并与某个已验收安装版本对应的最近一致性运行数据快照。最后有效快照不得仅因保留时间到期而自动删除。
_避免使用_：最新备份、自动备份

**发布代际**：
必须作为同一版本验证、切换和回退的三宿主插件、`publish.xml`、Adapter 程序、自启动或当前版本指针及其匹配运行数据快照集合。不同发布代际的组件不得混合标记为安装成功。
_避免使用_：Adapter 版本、插件版本、安装目录

**补偿式切换**：
跨多个目录无法由单一文件系统操作原子完成时，通过持久化事务日志、逐项同文件系统重命名和反向补偿，使安装中断或终检失败后能够恢复完整上一发布代际的切换方式。
_避免使用_：绝对原子升级、覆盖复制

**发布私有依赖**：
随某一发布代际安装和校验、只供该代际 Adapter 加载的 Python 依赖目录。候选预检和正式启动必须使用同一发布私有依赖，并禁止回退过程继续加载新代际或用户 `site-packages` 中的依赖。
_避免使用_：系统依赖、用户依赖、共享 Python 环境

**不可变发布目录**：
某一发布代际安装后不再由运行时修改的程序、脚本、发布私有依赖和清单目录。配置、密钥、数据库、日志、PID 和事务记录不得写入不可变发布目录。
_避免使用_：Adapter 安装目录、运行目录

**共享运行状态目录**：
位于发布目录之外、由当前发布代际通过明确路径读取的正式配置、API Key、任务映射和写作规范数据库目录。共享仅表示跨发布延续，不表示多个 Adapter 进程可以并发写入。
_避免使用_：程序配置目录、缓存目录

**白名单交付构建**：
从明确允许的安装器、三宿主插件、Adapter、目标依赖、清单、诊断验收恢复脚本、必要文档和许可证重新组装交付包，而不是复制旧包后逐项删除文件。未被白名单和发布清单引用的内容不得进入交付产物。
_避免使用_：交付目录清理、复制发布包

## 写作规范

**写作规范库**：
跨任务复用的组织级术语和写作要求集合，由术语规范、文体规则和去模板化规则组成。它不保存具体项目事实，也不模仿个人语言习惯。
_避免使用_：企业知识库、风格写法库

**术语规范**：
规定正式名称、允许别名、禁用变体及适用语境的组织级语言约束。
_避免使用_：专有名词条目

**文体规则**：
规定正式材料在结构、语气、责任表达和信息组织方面应遵循的正向要求。
_避免使用_：风格写法

**去模板化规则**：
用于识别并减少模板感、表演性语言和信息空转的表达要求，不用于判断作者身份，也不得削弱正式度、事实、术语、责任或规范性强度。
_避免使用_：去 AI 味、AI 检测规则

**预置基线**：
随产品发布并带有明确版本的通用写作规范集合。预置基线保持来源和原始内容可追溯，不因组织自定义而被直接改写。
_避免使用_：默认数据、示例数据

**规范来源**：
支撑一条预置基线的可追溯出处，包括来源名称、版本或标准编号。规范来源说明规则依据，不表示系统能够判断该依据对具体材料的法律适用性。
_避免使用_：参考链接

**短文本写作核心**：
从完整技术写作方法中提炼、适用于用户框选短文本的组织语体、保护项、去模板化和局部场景规则。短文本写作核心不包含多材料证据、项目追溯、技术正确性或法律适用性判断。
_避免使用_：完整技术写作能力

**组织覆盖**：
组织针对某条预置基线形成的替代、停用或补充要求。组织覆盖优先于对应预置基线，同时保留两者的来源关系。
_避免使用_：修改预置规则

**组织自定义规范**：
由组织自行新增且不依附于预置基线的写作规范。
_避免使用_：用户数据

**组织规范数据**：
组织维护的组织覆盖、组织自定义规范和预置停用状态。组织规范数据从首次正式安装开始持续复用，不因应用或预置规范包升级而被覆盖。
_避免使用_：运行缓存、安装数据

**生效规范**：
在指定任务中解析预置基线、组织覆盖和组织自定义规范后得到的最终适用要求。
_避免使用_：全部规则

**规范优先级**：
写作要求发生冲突时的固定裁决顺序：保护项、用户本次明确要求、组织覆盖与组织自定义规范、预置文体与术语基线、通用去模板化规则。
_避免使用_：模型自行判断

**预置停用**：
组织明确决定不采用某条预置基线的状态。预置停用保留基线本身及来源，只使其不进入生效规范。
_避免使用_：删除预置规则

**规范注入**：
将当前任务的生效规范加入既有模型请求，使模型在一次生成中同时遵守术语、文体、去模板化和保护要求。
_避免使用_：二次润色

**结果规范检查**：
对模型结果执行的本地非阻断检查，用于提示保护项变化和仍然存在的模板化表达线索。检查结果不判断作者身份，也不自动拒绝或再次改写模型结果。
_避免使用_：AI 检测、结果拦截

**规范降级**：
写作规范库部分或全部不可用时，三个 Word 任务继续执行但不完整应用规范，并向用户提示需要人工核对。规范降级不得清空、重建或覆盖组织规范数据。
_避免使用_：任务失败、空规范库

**需要核对**：
结果规范检查发现的保护项变化提示，涉及数字、标准编号、责任主体、规范性强度或其他可能改变事实和责任边界的内容。需要核对不自动判定结果错误。
_避免使用_：检查失败、结果错误

**表达建议**：
结果规范检查发现的模板感、表演性语言或信息空转线索。表达建议属于非阻断优化提示，不构成作者身份判断。
_避免使用_：AI 痕迹判定

**规范包**：
面向一种稳定写作边界发布和版本化的一组预置基线，例如 G 企技术写作基础、网络安全术语、技术文件文体或党政公文文体。规范包不是具体任务事实或固定成稿模板。
_避免使用_：规则文件、提示词模板

**规范包快照**：
随某一 AI-WPS 正式版本交付并经过兼容性验证的规范包集合。规范包快照在目标机离线使用，不通过运行时网络更新。
_避免使用_：在线规则、第三方规则包

**规范审阅清单**：
预置基线冻结前供人工核对的结构化条目集合，记录条目内容、规范包、类别、来源、版本、默认状态和审阅结论。未经确认的条目不进入默认启用基线。
_避免使用_：规范草稿、导入模板

**规范场景**：
决定一次任务应组合哪些规范包的用户可理解场景。规范场景描述材料的写作边界，不等同于模型工作流或 Word 功能。
_避免使用_：工作流类型、任务类型

**写作规范选择**：
用户在一次 Word 任务中选择的规范场景，包括自动匹配、明确场景或不使用写作规范。一次选择映射为一组规范包，不要求用户逐项勾选底层规则。
_避免使用_：规则多选、规范包多选

**适用范围**：
一条文体规则或去模板化规则可以生效的 Word 任务和规范场景组合。适用范围允许多选，不要求为跨任务或跨场景复用而复制规则。
_避免使用_：任务 scope、规则副本

**自动匹配**：
根据任务和输入内容保守选择规范场景；不能可靠判断时只采用 G 企技术写作基础规范，不推断为党政公文或网络安全材料。
_避免使用_：自动识别文种

**规范往返文件**：
从写作规范库导出并允许重新导入的 CSV 或 XLSX 文件。修改预置条目形成组织覆盖或预置停用，新增条目形成组织自定义规范，不直接改写预置基线。
_避免使用_：规范报表、数据库导出
