# Company Templates

Store company-specific Word formatting templates in this directory.

Each template should be a JSON file that overrides or extends the baseline structure in `templates/general/general-office.json`.
