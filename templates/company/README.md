# Company Templates

本目录不再作为运行时模板注册目录。格式审查唯一活动规则包为
`technical-document-template-rules`，其构建期输入位于
`packaging/format-rule-sources/`，编译结果位于
`adapter_service/format_rule_packs/`。

历史模板和规则资产保留在 `templates/history/inactive/`，仅用于审计和 Git
追溯，不得被运行时、界面或交付构建读取。
