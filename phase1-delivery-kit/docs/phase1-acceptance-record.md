# AI-WPS 一期交付验收记录

## 基本信息

- 版本：`v0.22.0-alpha`
- 版本规则号：`AI-WPS-P1-WORD-EXCEL-PPT-0.22.0-20260808`
- 交付包：`ai-wps-phase1-delivery-20260808-v0220.tar.gz`
- 发布清单：`release-manifest.json`
- 项目：
- 终端编号：
- 操作系统：
- CPU 架构：
- WPS 版本：
- Python 版本：
- 验收人员：
- 验收日期：

## 安装记录

- 交付包名称：
- 安装命令：
- WPS 插件目录：
- publish.xml 路径：
- Adapter 安装目录：
- Adapter 端口：
- Dify 用户输入模式（旧版/新版）：
- 首次安装规范库路径：
- 首次安装规范库权限：
- 首次安装规范库 SHA-256：

## 命令输出

### install_phase1.sh

```text

```

### phase1_smoke_test.sh

```text

```

## WPS 功能验证

- Ribbon 是否显示：
- 任务窗格是否显示：
- 模板下拉是否正常：
- 智能仿写是否正常：
- 文档审查是否正常：
- 格式审查是否正常：
- Excel“智能分析”是否正常：
- Excel 长任务等待和断连恢复是否正常：
- Excel“公式助手”生成公式是否正常：
- Excel“公式助手”解释排错是否正常：
- 30×20 上限、空选区和混合值/公式结果：
- 超长公式、外部引用、网络函数、越界引用、版本敏感函数和未知函数核对反馈：
- 公式助手独立工作流切换、慢模型排队和重开续查结果：
- 结构化主公式/说明和非结构化原始结果复制结果：
- 公式助手执行前后单元格值/公式摘要：
- 公式助手执行前后工作表名称/数量：
- 公式助手执行前后计算模式：
- PPT 智能总结当前页模式是否正常：
- PPT 智能总结 Markdown 文档模式是否正常：
- PPT 智能总结 DOCX 文档模式是否正常：
- PPT 无效 DOCX、非支持类型、超过 10 MB 拦截是否正常：
- PPT 5/8/10/12/15 页建议是否正常：
- PPT 当前页和文档结果复制操作是否正常：
- PPT 180 秒以上慢任务及重新打开任务窗格恢复是否正常：
- PPT 是否保持只读且未修改幻灯片：
- PPT 结构审查独立工作流切换结果：
- PPT 结构审查 60 页整套/显式页段边界结果：
- PPT 主副标题分离及无标题页 120 字符、10 页兜底结果：
- PPT 本地检查、单次模型调用、问题去重和分区结果：
- PPT 审查结论与推荐目录复制结果：
- PPT 结构审查前后幻灯片数量、顺序、主标题和副标题摘要：
- Word/Excel/PPT Ribbon 与工作流档案是否保持宿主隔离：
- 设置页工作流新建、修改、删除和功能页下拉即时切换是否正常：
- Word 写作规范新增、修改、删除和任务范围匹配是否正常：
- 写作规范 CSV/XLSX 预览导入、冲突跳过、CSV 导出和数据库备份是否正常：
- 写作规范库不可用时任务是否继续并显示降级提示：
- 单个预置包异常时其他规范与三个 Word 任务是否继续：
- 组织数据库不可读或迁移失败时主文件哈希与恢复备份是否保留：
- 写作规范诊断脱敏检查结果：
- 写作规范开发机性能最大耗时（目标 ≤100 ms）：
- 写作规范麒麟 V10 性能最大耗时（目标 ≤200 ms）：
- 320 px 窄窗、键盘、焦点和 reduced-motion 验收结果：
- 故障注入后的模型超时、长任务轮询、复制和写回回归结果：
- 覆盖安装后 API URL、统一 API Key 和全部任务级密钥是否保留：
- 覆盖安装后写作规范数据库和全部已有备份是否保留：
- 组织覆盖、组织自定义规范和预置停用状态重启后是否保持：
- 预置包升级后组织规范优先级是否保持：
- 二次覆盖安装后规范库 SHA-256：
- 二次覆盖安装后备份文件清单：
- Dify 新旧用户输入兼容是否正常：
- 智能编写是否正常：
- 应用预览是否正常：

## 交付包检查记录

- `release-manifest.json` 版本与版本规则号：本地审计通过，`0.22.0-alpha` / `AI-WPS-P1-WORD-EXCEL-PPT-0.22.0-20260808`
- 公式助手操作手册与提示词模板：本地审计通过
- 结构审查操作手册与提示词模板：本地审计通过
- 四个规范包及四份审阅清单：本地审计通过
- 来源与许可证说明：本地审计通过
- CSV/XLSX 空白模板：本地审计通过
- 现场数据库与备份排除：本地审计通过
- API Key、`adapter.json` 与日志排除：本地审计通过
- 用户导入内容与未确认草稿排除：本地审计通过
- Python 全量测试：555 项通过，55 项因当前 FastAPI/条件门禁跳过
- Node 全量测试：正式三宿主 13 个测试文件通过；`wps-addon` 4 个测试文件、11 个用例通过
- 三宿主 JavaScript 语法：10 个脚本通过
- TypeScript 类型检查：`tsc --noEmit` 通过
- Shell 语法：通过
- 浏览器布局：静态 layout smoke 回归通过；当前开发机未重复执行真实 Chromium 布局验收，沿用上一版本无结构改动基线并待目标机复核
- 构建产物大小与归档条目数：构建后在交付登记中填写；本记录位于归档内，不能自引用最终大小
- 构建产物 SHA-256：由构建脚本生成同名 `.sha256` 包外校验记录，以该文件为准

## 麒麟 V10 场景记录

| 场景 | 证据 | 结果 |
| --- | --- | --- |
| 三宿主 FIFO 排队与 2 个运行槽位 |  | 待目标机执行 |
| 满队列中文拒绝与排队取消 |  | 待目标机执行 |
| 短暂断连与任务窗格重开续查 |  | 待目标机执行 |
| Adapter 重启中断提示与重新提交 |  | 待目标机执行 |
| Word/Excel 选区事件与更新延迟 |  | 待目标机执行 |
| 可见页低频兜底与隐藏暂停 |  | 待目标机执行 |
| `HasFormula` 实际读取 |  | 待目标机执行 |
| `Formula` 实际读取 |  | 待目标机执行 |
| `FormulaLocal` 降级读取 |  | 待目标机执行 |
| `FormulaR1C1` 最终降级读取 |  | 待目标机执行 |
| 30×20、空选区和混合值/公式 |  | 待目标机执行 |
| 超长公式、外部引用、版本敏感函数和未知函数 |  | 待目标机执行 |
| 公式助手独立工作流、慢模型排队与重开续查 |  | 待目标机执行 |
| 公式助手复制与工作簿只读前后摘要 |  | 待目标机执行 |
| PPT Slides 集合与 60 页页段提取 |  | 待目标机执行 |
| PPT 主副标题分离与无标题页有限兜底 |  | 待目标机执行 |
| PPT 结构审查独立工作流、慢模型排队与重开续查 |  | 待目标机执行 |
| PPT 结构审查复制与幻灯片只读前后摘要 |  | 待目标机执行 |
| 首次安装与规范库初始化 |  | 待目标机执行 |
| 重启后组织规范持久化 |  | 待目标机执行 |
| 慢 `<think>` 模式与长任务轮询 |  | 待目标机执行 |
| 预置包/数据库/解析/检查故障降级 |  | 待目标机执行 |
| 再次覆盖安装与运行态保护 |  | 待目标机执行 |
| 格式审查、Excel、PPT、工作流、写回回归 |  | 待目标机执行 |

## 问题记录

| 序号 | 问题 | 现象截图 | 处理结论 |
| --- | --- | --- | --- |
| 1 |  |  |  |
| 2 |  |  |  |

## 验收结论

- 是否通过：未完成（本地自动化与构建门禁通过，麒麟 V10/WPS ET 真机验收待执行）
- 未完成原因：当前开发机不能替代 Issue #22 要求的 WPS 演示 Slides 集合、主副标题识别、60 页提取、慢模型和幻灯片只读真机证据。
- 后续动作：在目标终端逐项填写上述真机场景、前后摘要和最终结论；全部通过后再关闭 Issue #22。
